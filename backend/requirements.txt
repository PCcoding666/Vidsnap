# 阿里云视频分析平台依赖包
# 更新版本：使用阿里云 DashScope SenseVoice 进行音频转录

# Web框架
fastapi>=0.100.0
uvicorn>=0.23.0

# 视频处理
ffmpeg-python>=0.2.0
scenedetect[opencv]>=0.6.2  # PySceneDetect 场景检测库

# 云存储服务
oss2>=2.18.0

# AI服务
dashscope>=1.14.0  # 阿里云 DashScope SDK (SenseVoice + Qwen3-VL-Flash)

# 数据库服务
supabase>=2.10.0  # Supabase Python SDK (保留兼容性)

# 本地数据库 (PostgreSQL + SQLAlchemy)
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
greenlet>=3.0.0

# 认证系统
fastapi-users[sqlalchemy]>=13.0.0
httpx-oauth>=0.13.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# 基础工具
requests>=2.31.0
numpy>=1.24.0
Pillow>=10.0.0

# 环境变量管理
python-dotenv>=1.0.0

# 异步支持
asyncio>=3.4.3

# 可选：本地fallback（如果OpenAI服务不可用）
# SpeechRecognition>=3.10.0
# pydub>=0.25.1

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0

# 任务队列 (频道监控功能)
celery>=5.3.0
redis>=4.5.0
kombu>=5.3.0

# 表单解析
python-multipart>=0.0.6

# 邮箱验证
email-validator>=2.0.0

# 系统监控
psutil>=5.9.0

# YouTube 下载（FetchYouTube skill）
yt-dlp>=2024.1.0
