# 阿里云视频分析平台依赖包
# 更新版本：使用阿里云 DashScope SenseVoice 进行音频转录

# Web框架
fastapi>=0.100.0
uvicorn>=0.23.0

# Web界面
gradio>=4.0.0
httpx[socks]>=0.24.0  # 支持 SOCKS 代理

# 视频处理 
yt-dlp>=2024.10.7  # 更新到最新版本以支持最新的 YouTube 反爬虫机制
ffmpeg-python>=0.2.0
scenedetect[opencv]>=0.6.2  # PySceneDetect 场景检测库

# 云存储服务
oss2>=2.18.0

# AI服务
dashscope>=1.14.0  # 阿里云 DashScope SDK (SenseVoice + Qwen3-VL-Flash)

# 基础工具
requests>=2.31.0
numpy>=1.24.0
Pillow>=10.0.0

# 环境变量管理
python-dotenv>=1.0.0

# 异步支持
asyncio>=3.4.3

# 可选：本地fallback（如果OpenAI服务不可用）
# SpeechRecognition>=3.10.0
# pydub>=0.25.1

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0